version: '3.7'

services:
  app:
    build: .
    environment:
      - DB_HOST=database
      - DB_NAME=database
      - DB_USER=database
      - DB_PASSWORD=database
    ports:
      - "5000:5000"





